---
"louise-toolkit": patch
---

The soft-lock badge on a field another editor holds now shows a Phosphor lock icon instead of an emoji. The badge reads "Alex is editing", and the icon comes from CSS. If you style `[data-louise-locked-by]` yourself, the attribute no longer starts with an emoji.
